<Page x:Class="WPF_CRYPTO.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_CRYPTO.Views" xmlns:viewmodels="clr-namespace:WPF_CRYPTO.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:SettingsPageModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>-->
        <TextBlock Grid.Row="0" Text="Settings" Margin="5" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
        <Button Grid.Row="1" Content="Dark Theme" Margin="5" Command="{Binding LightThemeCommand}" Width="200" HorizontalAlignment="Left"/>
        <Button Grid.Row="1" Content="Light Theme" Margin="5" Command="{Binding DarkThemeCommand}" Width="200" HorizontalAlignment="Right"/>
    </Grid>
</Page>
